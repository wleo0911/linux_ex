#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define mem_size 200

char *pData="hello World";

void main(void)
{
	char a[20];
	
	printf("%s %p\n", pData, pData);	



	pData=malloc(sizeof(char)*mem_size);

	printf("%p\n", pData);

	

	
	strncpy(pData, "change", sizeof(pData));

	printf("%s %p\n", pData, pData);

		
}

